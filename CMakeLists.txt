CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

PROJECT(EPOLLServer)

#AUX_SOURCE_DIRECTORY(./ SRC_LIST)
SET(SRC_LIST main.cpp Server.cpp ThreadPool.h)

ADD_EXECUTABLE(Server ${SRC_LIST})

TARGET_LINK_LIBRARIES(Server pthread)
